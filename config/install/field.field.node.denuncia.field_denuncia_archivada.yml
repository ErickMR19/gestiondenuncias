langcode: es
status: true
dependencies:
  config:
    - field.storage.node.field_denuncia_archivada
    - node.type.denuncia
id: node.denuncia.field_denuncia_archivada
field_name: field_denuncia_archivada
entity_type: node
bundle: denuncia
label: 'Denuncia archivada'
description: 'Si la denuncia está archivada no le aparece al usuario encargado, y en caso de tener respuestas pendientes, no se enviaran correos'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: Archivada
  off_label: Activa
field_type: boolean
